import styled from 'styled-components'
import { colors } from 'utils/variables'

export const Hr = styled.div`
  border-top: 2px dotted ${colors.hunterOrange};
  width: 8rem;
  max-width: 100%;
  display: block;
  height: 0px;
  margin: 24px 0;
`
